# Speculative Decoding 图表清单与 QA

> [!info] 文档关系
> - 文档类型：Evidence
> - 领域入口：[README](../README.md)
> - 上位汇总：[Evolution](../surveys/evolution.md)
> - 证据资产：`../assets/papers/`
> - 相关文档：[Foundations and trends](../surveys/foundations-and-trends.md)

本清单索引九篇 canonical Paper 的正式视觉证据，不替代逐篇 Paper 的实验解释。论文图保留完整 caption、单一编号对象、源页尺寸与 crop bbox；TorchSpec 的博客图是未编号的 standalone official objects，保留完整 image bounds。所有资产均通过 contact-sheet 初筛和逐图原分辨率复核；页面渲染、裁剪过程与 QA 日志仅保存在过程工作区。

| AngelSpec / Figure 2 | `../assets/papers/angelspec/dfly-overview.png` | DFly mechanism overview | pass：原分辨率 QA |
| AngelSpec / Figure 3 | `../assets/papers/angelspec/dcut-overview.png` | D-cut dynamic verification budget | pass：原分辨率 QA |
| AngelSpec / Table 7 | `../assets/papers/angelspec/throughput-table7.png` | Serving throughput comparison | pass：原分辨率 QA |
| AngelSpec / Figure 6 | `../assets/papers/angelspec/framework-overview.png` | AngelSpec training/serving framework | pass：原分辨率 QA |
| TorchSpec / official architecture | `../assets/papers/torchspec/disaggregated-architecture.png` | Disaggregated inference/training architecture | pass：source-bounds QA |
| TorchSpec / official Kimi benchmark | `../assets/papers/torchspec/kimi-draft-benchmark.png` | Draft quality benchmark | pass：source-bounds QA |
| TorchSpec / official throughput | `../assets/papers/torchspec/kimi-throughput.png` | EAGLE3 serving throughput | pass：source-bounds QA |
| TorchSpec / official max sequence | `../assets/papers/torchspec/max-sequence-length.png` | Maximum sequence length | pass：source-bounds QA |

| Paper / 对象与 source crop | 正式资产 | 完整 caption | 用途 / Provenance | QA |
|---|---|---|---|---|
| DFlash | Figure 1 / p.2；源页 `2040×2640`；crop `(175,225,1665,625)` | `../assets/papers/dflash/fig1-speedup-caption.png` | “Figure 1. Speedup comparison between DFlash, EAGLE-3 against Autoregressive Decoding on Qwen3-8B (Yang et al., 2025) with the Transformers backend. Overall, DFlash achieves more than 2.5× higher speedup than EAGLE-3.” | pass：单一编号对象、完整 caption，2026-07-25 contact-sheet + 原分辨率 QA |
| DFlash | Figure 2 / p.4；源页 `2040×2640`；crop `(175,215,1650,760)` | `../assets/papers/dflash/fig2-inference-design-caption.png` | “Figure 2. DFlash Inference Design. Hidden context features extracted from the target model are fused and injected into each draft layer's Key-Value cache to enable conditional speculation.” | pass：机制图单一对象、完整 caption，2026-07-25 QA |
| DFlash | Figure 3 / p.3；源页 `2040×2640`；crop `(1014,1810,801,510)` | `../assets/papers/dflash/fig3-draft-latency-caption.png` | “Figure 3. Draft cost of 1, 3, 5-layer DFlash and 1-layer EAGLE-3.” | pass：结果图单一对象、完整 caption，2026-07-25 QA |
| DFlash | Figure 4 / p.5；源页 `2040×2640`；crop `(175,220,800,925)` | `../assets/papers/dflash/fig4-training-attention-caption.png` | “Figure 4. DFlash training attention. The target model provides context features (blue) that condition the draft model. The input consists of clean prompt tokens p and clean response tokens r. Within each masked block, a subset of clean response tokens (yellow) is randomly sampled as anchors, while mask tokens m (green) mark positions for parallel prediction. Invisible tokens (white) denote the attention mask, which enforces causal consistency and prevents inter-block information leakage during training.” | pass：首次 crop 因页眉/截断被拒后重裁；2026-07-25 QA |
| DFlash | Figure 5 / p.13；源页 `2040×2640`；crop `(175,465,805,530)` | `../assets/papers/dflash/fig5-acceptance-vs-epoch-caption.png` | “Figure 5. The loss decay makes training converge faster and better.” | pass：单一编号对象、完整 caption，2026-07-25 QA |
| D²SD | Figure 1 / p.4；源页 `2550×3300`；crop `(280,360,1995,1110)` | `../assets/papers/d2sd/fig1_pipeline_caption.png` | “Figure 1. (a) Baseline DFlash on a γ=4 block. The DFlash drafter consumes the bonus-token anchor at position 0 and γ−1 mask positions, and predicts all mask positions in a single parallel pass conditioned on target hidden features via KV injection. The target model then verifies the drafted block by prefix matching, accepting tokens up to (but not including) the first mismatch and discarding the rest. (b) Our D²SD pipeline on the same γ=4 block with K=2 branches. (1) DFlash (first draft) runs as in (a), producing a first draft together with per-position confidences. (2) Top-K Unmask scores every prefix length and selects the K most-likely rejection boundaries, yielding K variable-prefix batches. (3) VP-Drafter fills in all K batches in one batched forward pass. (4) Target Model jointly verifies the K+1 candidates using shared-prefix cascade attention and commits the longest accepted prefix.” | pass：单一编号对象、完整 caption，2026-07-25 contact-sheet + 原分辨率 QA |
| D²SD | Figure 2 / p.6；源页 `2550×3300`；crop `(280,315,1995,800)` | `../assets/papers/d2sd/fig2_confidence_topk_caption.png` | “Figure 2. (a) DFlash drafter confidence versus empirical accept rate. Each bar is the mean accept rate of draft tokens whose confidence falls into the corresponding bin; the red dashed line is y=x. The drafter is near-diagonally calibrated and slightly conservative in the high-confidence regime. (b) Top-K unmask with K=4 on a γ=6 block: every candidate prefix length is scored and the four highest are selected. Each selected prefix yields one second-draft input that retains the prefix and re-masks the rest; all branches are batched into one VP-Drafter forward pass.” | pass：双 panel 同属 Figure 2、完整共享 caption，2026-07-25 QA |
| D²SD | Table 3 / p.8；源页 `2550×3300`；crop `(280,315,1995,930)` | `../assets/papers/d2sd/table3_qwen_main_caption.png` | “Table 3. Wall-clock speedup over autoregressive decoding and mean acceptance length (α) on Qwen3-8B (γ=16, K=4) under greedy (T=0) and sampling (T=1) decoding. EAGLE-3, DFlash, and D²SD are measured on the same public Qwen3-8B checkpoint with matched hardware and decoding hyperparameters; the authors report end-to-end wall-clock speedup and mean acceptance length from their runs at both temperatures. Row-wise averages are arithmetic means over the eight datasets.” | pass：单一表格、完整 caption，2026-07-25 QA |
| D²SD | Table 5 / p.9；源页 `2550×3300`；crop `(280,415,1995,550)` | `../assets/papers/d2sd/table5_resampling_ablation_caption.png` | “Table 5. Augmenting argmax DFlash with K=4 resampled branches yields only modest gains over single-branch DFlash, while D²SD improves both metrics substantially under the same branching budget. Greedy target decoding (T=0); each cell is speedup × / mean acceptance length α.” | pass：单一表格、完整 caption，2026-07-25 QA |
| DSpark / Figure 1 / p.5；源页 `2481×3508`；crop `(280,350,1920,1680)` | `../assets/papers/dspark/fig1-architecture-caption.png` | “The DSpark architecture and decoding cycle. Given prompt tokens ABC, the target model executes one step to generate the next token D, which serves as the anchor for the drafting phase. Using D as the input, DSpark employs a heavy parallel backbone and a lightweight sequential head to generate draft tokens EFGH along with their corresponding confidence scores c1–c4. The Hardware-Aware Prefix Scheduler then evaluates these scores to retain the prefix EFG and drop the low-confidence token H. Finally, the target model verifies the scheduled prefix in parallel. As illustrated, E and F are accepted while G is rejected, prompting the model to generate a corrected token G* to complete the current round.” | 端到端 drafting/scheduling/verification 机制 | pass：重裁后 contact-sheet + 原分辨率 QA |
| DSpark / Table 1 / p.11；源页 `2481×3508`；crop `(290,330,1900,905)` | `../assets/papers/dspark/table1-main-results-caption.png` | “Main speculative decoding results. We report accepted length (τ) per decoding round (higher is better) for different target models and domains. Bold marks the best results.” | 固定 block 离线协议主结果 | pass：去除多余空白后逐图 QA |
| DSpark / Figure 2 / p.12；源页 `2481×3508`；crop `(280,350,1910,875)` | `../assets/papers/dspark/fig2-conditional-acceptance-caption.png` | “Position-wise conditional acceptance. We report the empirical conditional acceptance rate for each draft position, averaged across benchmarks within each domain using the Qwen3-4B target model. Unlike standard prefix survival, this metric isolates the baseline predictive quality at position k by removing the penalty of previous rejections. Notice that the autoregressive drafter (Eagle3) remains stable or trends upward, while the parallel drafter (DFlash) suffers suffix decay.” | suffix decay 与 sequential head 动机 | pass：2026-07-25 原分辨率 QA |
| DSpark / Figure 3 / p.13；源页 `2481×3508`；crop `(280,340,1910,945)` | `../assets/papers/dspark/fig3-drafter-depth-caption.png` | “Effect of drafter depth. With proposal length fixed, DSpark's performance improves as drafter layers are added. Notably, a shallow 2-layer DSpark outperforms a deeper 5-layer DFlash baseline, highlighting the parameter efficiency of sequential modeling.” | drafter depth 消融 | pass：caption/空白重裁后 QA |
| DSpark / Figure 4 / p.13；源页 `2481×3508`；crop `(280,1295,1910,700)` | `../assets/papers/dspark/fig4-proposal-latency-caption.png` | “Effect of proposal length and latency overhead. DSpark consistently outperforms DFlash across various block sizes (left three panels). The rightmost panel demonstrates that the sequential head introduces minimal latency overhead during serving.” | proposal length 与 latency | pass：排除上一对象 caption 后 QA |
| DSpark / Figure 5 / p.15；源页 `2481×3508`；crop `(280,340,1910,835)` | `../assets/papers/dspark/fig5-confidence-sweep-caption.png` | “Confidence threshold sweep. A threshold of 0 corresponds to standard fixed-length verification. As the threshold increases, the overall acceptance rate steadily rises because the confidence head effectively prunes tokens that would ultimately be rejected (hashed bars).” | confidence threshold diagnostic | pass：2026-07-25 原分辨率 QA |
| DSpark / Figure 6 / p.15；源页 `2481×3508`；crop `(280,1205,1910,945)` | `../assets/papers/dspark/fig6-reliability-caption.png` | “The Reliability Diagram on Alpaca Dataset. While the raw confidence estimator achieves strong discrimination, its predictions are inherently overconfident. Applying post-hoc calibration helps to align the prefix survival probabilities with empirical acceptance rates. The shaded background histogram represents the frequency distribution of sample counts across different confidence bins.” | calibration / STS 边界 | pass：caption 重裁后 QA |
| DSpark / Figure 7 / p.18；源页 `2481×3508`；crop `(280,350,1910,980)` | `../assets/papers/dspark/fig7-live-frontier-caption.png` | “Throughput vs. TPS. Aggregate output token throughput against per-request generation speed (tok/s/user) under live traffic. In our production deployment, DSpark improves the observed throughput–interactivity frontier relative to the MTP-1 baseline under the measured traffic and engine configurations.” | 线上 throughput–interactivity frontier | pass：caption/空白重裁后 QA |
| DSpark / Figure 8 / p.19；源页 `2481×3508`；crop `(280,950,1910,1350)` | `../assets/papers/dspark/fig8-load-adaptive-caption.png` | “Load-adaptive throughput and verification budgets. Top row (a, b): Aggregate output throughput across varying levels of system concurrency. Bottom row (c, d): The average target verification budget allocated per request. As concurrent load increases, the dynamic scheduler automatically restricts the per-request verification length to prevent resource contention.” | 负载自适应预算；仅支持 whole-stack 归因 | pass：排除后续正文后 QA |
| DeLS-Spec / Figure 1 / p.4；源页 `2481×3508`；crop bbox `(430,280,2130,1450)` | `../assets/papers/dels-spec/fig1-overview-caption.png` | “Overview of DeLS-Spec. DFlash produces long-context logits in parallel, while a lightweight local head sequentially estimates short-context logits from the draft prefix. DeLS-Spec fuses them with a unigram prior to obtain final draft logits, which are sampled and then verified by the target model under standard speculative decoding.” | 长/短上下文专家、unigram correction 与 target verification 总体机制 | pass：单一编号对象、完整 caption；2026-07-28 contact-sheet + 原分辨率 QA |
| DeLS-Spec / Table 1 / p.7；源页 `2481×3508`；crop bbox `(430,330,2130,1405)` | `../assets/papers/dels-spec/table1-main-results-caption.png` | “Decoding speedup over vanilla autoregressive decoding and average acceptance length ($\tau$) on Qwen3 models with a maximum of 2048 generated tokens. Parenthesized values indicate the draft tree size for EAGLE-3 and DART, and the draft block size for DFlash and DeLS-Spec. The average is computed over all listed benchmarks.” | 4B/8B、温度 0/1 主结果 | pass：完整宽表、caption 与全部单元格可读；2026-07-28 原分辨率 QA |
| DeLS-Spec / Table 2 / p.7；源页 `2481×3508`；crop bbox `(430,1430,2130,1830)` | `../assets/papers/dels-spec/table2-dspark-dflash-transfer-caption.png` | “Results of directly applying DeLS-Spec to DSpark's DFlash block-7 checkpoints. DeLS-Spec achieves consistent performance improvements across all benchmarks.” | 对 DSpark 发布的 DFlash b7 baseline checkpoint 的直接迁移证据；不代表完整 DSpark 对比 | pass：单一表格、完整 caption；2026-07-28 原分辨率 QA |
| DeLS-Spec / Figure 2 / p.8；源页 `2481×3508`；crop bbox `(430,280,2130,930)` | `../assets/papers/dels-spec/fig2-alpha-beta-scan-caption.png` | “$\alpha,\beta$ scan on Qwen3-4B. Left: average acceptance length under different $\alpha$ and $\beta$ values. Middle: row-wise improvement after subtracting the corresponding $\beta=0$ value. Right: improvement of the best $\beta$ for each $\alpha$ over $\beta=0$.” | local correction 与 unigram prior subtraction 的敏感性证据 | pass：三个 panel 同属 Figure 2、完整共享 caption；2026-07-28 QA |
| DeLS-Spec / Table 3 / p.8；源页 `2481×3508`；crop bbox `(430,1010,2130,1510)` | `../assets/papers/dels-spec/table3-residual-tradeoff-caption.png` | “Average acceptance length comparison for estimating the impact of the omitted residual term. The gain ratio reports how much of the Domino-FT improvement over DFlash is recovered by DeLS-Spec without explicitly learning the residual term.” | Markov/RNN 与 residual-learning bridge baseline 的质量权衡 | pass：单一表格、完整 caption 与 gain-ratio 行；2026-07-28 QA |
| DeLS-Spec / Table 5 / p.9；源页 `2481×3508`；crop bbox `(430,730,2130,1320)` | `../assets/papers/dels-spec/table5-training-cost-caption.png` | “Training cost comparison on a single NVIDIA L20 GPU (48G). DeLS-Spec local heads require much less training time and memory than Domino-FT, with the Markov variant being the cheapest due to dense supervision and its minimal parameterization.” | 单卡训练时间与 VRAM 成本 | pass：单一表格、完整 caption 与 4B/8B 行；2026-07-28 QA |
| HyperDFlash / Figure 1 / p.2；源页 `1530×1980`；crop `(170,240,1195,400)` | `../assets/papers/hyperdflash/fig1-position-acceptance-caption.png` | “Per-position acceptance rates over six drafted positions. Native MTP achieves high acceptance at the first position but degrades rapidly at later positions, while HyperDFlash maintains a smoother acceptance profile.” | 后位 acceptance 衰减与方法动机 | pass：单一编号对象、完整 caption，2026-07-25 contact-sheet + 原分辨率 QA |
| HyperDFlash / Figure 2 / p.3；源页 `1530×1980`；crop `(170,245,1190,555)` | `../assets/papers/hyperdflash/fig2-overview-caption.png` | “Overview of HyperDFlash. The drafter conditions on the target model’s final pre-collapse HC residual, which is reduced by the proposed Inherited HC-Gate Reducer and injected into a lightweight DFlash drafter. An auxiliary KL distillation objective further improves early-position draft quality.” | pipeline 与组件 rationale | pass：完整图例、箭头和 caption，2026-07-25 QA |
| HyperDFlash / Table 1 / p.4；源页 `1530×1980`；crop `(170,240,1195,285)` | `../assets/papers/hyperdflash/table1-reducer-design-caption.png` | “Design comparison of the path-collapse reducers for DeepSeek-V4-Flash. The Inherited HC-Gate Reducer follows the input-dependent HC path-gating form of the target model, delivering high parameter efficiency via architectural alignment rather than standalone feature compression.” | reducer 结构与参数量 | pass：单一表格、完整 caption，2026-07-25 QA |
| HyperDFlash / Table 2 / p.5；源页 `1530×1980`；crop `(170,240,1190,385)` | `../assets/papers/hyperdflash/table2-nonthinking-results-caption.png` | “Main decoding results for DeepSeek-V4-Flash in Non-thinking mode. We report decoding speedup over target-only autoregressive decoding and average accepted length (τ). Parenthesized values indicate the number of drafted steps per verification round.” | Non-thinking 主结果与收益归因 | pass：所有单元格与完整 caption 可读，2026-07-25 QA |
| HyperDFlash / Table 3 / p.5；源页 `1530×1980`；crop `(170,635,1190,390)` | `../assets/papers/hyperdflash/table3-thinkhigh-results-caption.png` | “Main decoding results for DeepSeek-V4-Flash in Think-high mode. We report decoding speedup over target-only autoregressive decoding and average accepted length (τ). Parenthesized values indicate the number of drafted steps per verification round.” | Think-high 跨温度结果 | pass：所有单元格与完整 caption 可读，2026-07-25 QA |
| JetSpec / Figure 3 / p.4；源页 `2550×3300`；crop `(430,300,1690,670)` | `../assets/papers/jetspec/fig3-jetspec-design-caption.png` | “JetSpec design overview. JetSpec extracts fused hidden features from the frozen target model and conditions a causal-parallel draft head to generate high-quality candidate trees in one forward pass.” | causal-parallel head 与 tree verification | pass：单一编号对象、完整 caption，2026-07-25 contact-sheet + 原分辨率 QA |
| JetSpec / Table 2 / p.7；源页 `2550×3300`；crop `(440,1260,1670,965)` | `../assets/papers/jetspec/table2-high-budget-results-caption.png` | “High-budget comparison on Qwen3-8B with at least 64 draft tokens. EAGLE-3 uses tree mode with max depth 8; larger budgets give minimal or worse gains due to training mismatch.” | high-budget 主结果 | pass：单一表格、完整 caption，2026-07-25 QA |
| JetSpec / Table 7 / p.10；源页 `2550×3300`；crop `(440,285,1670,390)` | `../assets/papers/jetspec/table7-causal-diffusion-gamma-ablation-caption.png` | “Architecture and γ ablation on MATH-500 with JetSpec at LR 3×10⁻⁴ with forward-KL distillation. We report speedup and average accepted length τ.” | causal/diffusion head 与 γ 消融 | pass：单一表格、完整 caption，2026-07-25 QA |
| JetSpec / Figure 4 / p.15；源页 `2550×3300`；crop `(440,580,1670,1155)` | `../assets/papers/jetspec/fig4-tree-quality-failure-caption.png` | “Tree-quality failure mode at MATH-500 prompt #0, decode step 0. Both heads draft from the same prefix (last token ‘We’). The causal head’s rank-1 branch (‘ are told that’) is faithful: target joint Σ log p ≈ Σ log r, so tree verification walks 6 tokens along it. The diffusion head’s rank-1 branch (‘ given told that’) is incoherent (target joint Σ log p = −63.32 nats, i.e. probability ≈ e⁻⁶³) because its branch-agnostic per-position predictor composes ‘ given’ (depth 1) and ‘ told’ (depth 2) independently in the surrogate, even though no real continuation places these two words consecutively (the surrogate q_sur is formally defined in Equation 3). The actually-coherent ‘ are given that the’ is in the diffusion tree but only at rank 3; the surrogate fails to promote it, and the verifier accepts only 4 tokens.” | branch-agnostic tree failure 与 rank-1 边界 | pass：所有 panel 与完整 caption 可读，2026-07-25 QA |
| P-EAGLE / Figure 1 / p.2；源页 `2040×2640`；crop `(180,225,800,585)` | `../assets/papers/p-eagle/fig1-sequence-length-caption.png` | “Sequence length (prompt + generation) distribution on UltraChat dataset with GPT-OSS 120B. Reasoning level: Medium. Median: 3,891 tokens; P90: 10,800 tokens; P99: 20,000 tokens.” | 长输出训练触发条件 | pass：轴、图例与完整 caption 可读，2026-07-25 QA |
| P-EAGLE / Figure 2 / p.2；源页 `2040×2640`；crop `(1015,225,790,1210)` | `../assets/papers/p-eagle/fig2-architecture-caption.png` | “P-EAGLE architecture. The target model (top) processes prompt tokens and produces hidden states from layer indexes 2, L/2, and L−1 (concatenated to 3d dimensions), where L is the number of decoder layers. The P-EAGLE drafter (bottom) takes these hidden states for the Next-Token Prediction (NTP) position (Pos 1), which operates like standard autoregressive prediction with actual context. Multi-Token Prediction (MTP) positions (Pos 2-4) use a learnable shared hidden state h_shared since they lack preceding hidden states. Token embeddings are combined with projected hidden states and processed through N transformer layers.” | parallel MTP 架构 | pass：完整 diagram、legend 与 caption，2026-07-25 QA |
| P-EAGLE / Figure 3 / p.3；源页 `2040×2640`；crop `(1020,215,790,575)` | `../assets/papers/p-eagle/fig3-mask-slicing-caption.png` | “Position-invariance of causal attention across prediction depths. G0, G1, G2 in the figure denote prediction depths 0, 1, 2, where depth d predicts the token d+1 positions ahead. The mask for a shorter sequence (right) is exactly the top-left submatrix of a longer sequence's mask (left), enabling constant-time retrieval.” | amortized mask construction | pass：全部标签与完整 caption 可读，2026-07-25 QA |
| P-EAGLE / Figure 4 / p.4；源页 `2040×2640`；crop `(1015,220,800,755)` | `../assets/papers/p-eagle/fig4-sequence-partition-caption.png` | “Sequence partitioning for within-sequence gradient accumulation. Example: n=16 tokens, K=4 prediction depths (G0–G3 denote groups in PARD terminology, where group g predicts the token g+1 positions ahead). Depth 0 contains all positions; depths 1–3 contain progressively fewer due to COD sampling. Partitioning by depth-0 indices causes dependency violations: position 8 at depth 2 depends on position 7 at depth 1, but they land in different segments. Our algorithm tracks assignments iteratively across depths to preserve dependencies.” | dependency-preserving splitting | pass：依赖箭头、图例与完整 caption 可读，2026-07-25 QA |
| P-EAGLE / Table 9 / p.7；源页 `2040×2640`；crop `(1020,215,790,630)` | `../assets/papers/p-eagle/table9-acceptance-length-caption.png` | “Acceptance length comparison across three models on three out-of-distribution benchmarks. Speculation depth K=5, max new tokens 2048. Percentages denote improvement over AR EAGLE-3.” | acceptance quality 主结果 | pass：单一表格、完整 caption，2026-07-25 QA |
| P-EAGLE / Table 10 / p.8；源页 `2040×2640`；crop `(180,220,1640,1000)` | `../assets/papers/p-eagle/table10-otps-caption.png` | “Output Tokens Per Second (OTPS) across speculation depths K and concurrency levels (C). At each K, OTPS measures total throughput across all concurrent requests. Underline indicates AR baseline (optimal K). P-EAGLE speedups (in parentheses) are relative to this baseline; bold indicates best speedup. HE=HumanEval, MT=MT-Bench, GSM=GSM-8K. All experiments use chain drafting and are measured on 1 H200 GPU.” | OTPS、并发与收益归因 | pass：宽表所有单元格与完整 caption 可读，2026-07-25 QA |
