# Speculative Decoding 图表清单与 QA

> [!info] 文档关系
> - 文档类型：Evidence
> - 领域入口：[README](../README.md)
> - 上位汇总：[Evolution](../surveys/evolution.md)
> - 证据资产：`../assets/papers/`
> - 相关文档：[Foundations and trends](../surveys/foundations-and-trends.md)

本清单为 legacy formal assets 建立正式索引。它不替代逐篇 Paper 的实验解释。现有 assets 来自旧精读导出，但 source PDF page render、完整原 caption 与精确 crop bbox 未统一保留；因此下表将视觉对象、用途和缺口显式分类为 `blocked`，避免把不完整的历史 provenance 伪装成 QA 通过。恢复材料后，过程文件只能进入过程工作区，通过原分辨率复核的单一编号对象才可将本表更新为 `pass`。

| Paper / 对象与 source crop | 正式资产 | 完整 caption | 用途 / Provenance | QA |
|---|---|---|---|---|
| DFlash | Figure 1 / p.2；源页 `2040×2640`；crop `(175,225,1665,625)` | `../assets/papers/dflash/fig1-speedup-caption.png` | “Figure 1. Speedup comparison between DFlash, EAGLE-3 against Autoregressive Decoding on Qwen3-8B (Yang et al., 2025) with the Transformers backend. Overall, DFlash achieves more than 2.5× higher speedup than EAGLE-3.” | pass：单一编号对象、完整 caption，2026-07-25 contact-sheet + 原分辨率 QA |
| DFlash | Figure 2 / p.4；源页 `2040×2640`；crop `(175,215,1650,760)` | `../assets/papers/dflash/fig2-inference-design-caption.png` | “Figure 2. DFlash Inference Design. Hidden context features extracted from the target model are fused and injected into each draft layer's Key-Value cache to enable conditional speculation.” | pass：机制图单一对象、完整 caption，2026-07-25 QA |
| DFlash | Figure 3 / p.3；源页 `2040×2640`；crop `(1014,1810,801,510)` | `../assets/papers/dflash/fig3-draft-latency-caption.png` | “Figure 3. Draft cost of 1, 3, 5-layer DFlash and 1-layer EAGLE-3.” | pass：结果图单一对象、完整 caption，2026-07-25 QA |
| DFlash | Figure 4 / p.5；源页 `2040×2640`；crop `(175,220,800,925)` | `../assets/papers/dflash/fig4-training-attention-caption.png` | “Figure 4. DFlash training attention. The target model provides context features (blue) that condition the draft model. The input consists of clean prompt tokens p and clean response tokens r. Within each masked block, a subset of clean response tokens (yellow) is randomly sampled as anchors, while mask tokens m (green) mark positions for parallel prediction. Invisible tokens (white) denote the attention mask, which enforces causal consistency and prevents inter-block information leakage during training.” | pass：首次 crop 因页眉/截断被拒后重裁；2026-07-25 QA |
| DFlash | Figure 5 / p.13；源页 `2040×2640`；crop `(175,465,805,530)` | `../assets/papers/dflash/fig5-acceptance-vs-epoch-caption.png` | “Figure 5. The loss decay makes training converge faster and better.” | pass：单一编号对象、完整 caption，2026-07-25 QA |
| D²SD | Figure 1 / p.4；源页 `2550×3300`；crop `(280,360,1995,1110)` | `../assets/papers/d2sd/fig1_pipeline_caption.png` | “Figure 1. (a) Baseline DFlash on a γ=4 block. The DFlash drafter consumes the bonus-token anchor at position 0 and γ−1 mask positions, and predicts all mask positions in a single parallel pass conditioned on target hidden features via KV injection. The target model then verifies the drafted block by prefix matching, accepting tokens up to (but not including) the first mismatch and discarding the rest. (b) Our D²SD pipeline on the same γ=4 block with K=2 branches. (1) DFlash (first draft) runs as in (a), producing a first draft together with per-position confidences. (2) Top-K Unmask scores every prefix length and selects the K most-likely rejection boundaries, yielding K variable-prefix batches. (3) VP-Drafter fills in all K batches in one batched forward pass. (4) Target Model jointly verifies the K+1 candidates using shared-prefix cascade attention and commits the longest accepted prefix.” | pass：单一编号对象、完整 caption，2026-07-25 contact-sheet + 原分辨率 QA |
| D²SD | Figure 2 / p.6；源页 `2550×3300`；crop `(280,315,1995,800)` | `../assets/papers/d2sd/fig2_confidence_topk_caption.png` | “Figure 2. (a) DFlash drafter confidence versus empirical accept rate. Each bar is the mean accept rate of draft tokens whose confidence falls into the corresponding bin; the red dashed line is y=x. The drafter is near-diagonally calibrated and slightly conservative in the high-confidence regime. (b) Top-K unmask with K=4 on a γ=6 block: every candidate prefix length is scored and the four highest are selected. Each selected prefix yields one second-draft input that retains the prefix and re-masks the rest; all branches are batched into one VP-Drafter forward pass.” | pass：双 panel 同属 Figure 2、完整共享 caption，2026-07-25 QA |
| D²SD | Table 3 / p.8；源页 `2550×3300`；crop `(280,315,1995,930)` | `../assets/papers/d2sd/table3_qwen_main_caption.png` | “Table 3. Wall-clock speedup over autoregressive decoding and mean acceptance length (α) on Qwen3-8B (γ=16, K=4) under greedy (T=0) and sampling (T=1) decoding. EAGLE-3, DFlash, and D²SD are measured on the same public Qwen3-8B checkpoint with matched hardware and decoding hyperparameters; the authors report end-to-end wall-clock speedup and mean acceptance length from their runs at both temperatures. Row-wise averages are arithmetic means over the eight datasets.” | pass：单一表格、完整 caption，2026-07-25 QA |
| D²SD | Table 5 / p.9；源页 `2550×3300`；crop `(280,415,1995,550)` | `../assets/papers/d2sd/table5_resampling_ablation_caption.png` | “Table 5. Augmenting argmax DFlash with K=4 resampled branches yields only modest gains over single-branch DFlash, while D²SD improves both metrics substantially under the same branching budget. Greedy target decoding (T=0); each cell is speedup × / mean acceptance length α.” | pass：单一表格、完整 caption，2026-07-25 QA |
| DSpark / Figure 1 / p.5；源页 `2481×3508`；crop `(280,350,1920,1680)` | `../assets/papers/dspark/fig1-architecture-caption.png` | “The DSpark architecture and decoding cycle. Given prompt tokens ABC, the target model executes one step to generate the next token D, which serves as the anchor for the drafting phase. Using D as the input, DSpark employs a heavy parallel backbone and a lightweight sequential head to generate draft tokens EFGH along with their corresponding confidence scores c1–c4. The Hardware-Aware Prefix Scheduler then evaluates these scores to retain the prefix EFG and drop the low-confidence token H. Finally, the target model verifies the scheduled prefix in parallel. As illustrated, E and F are accepted while G is rejected, prompting the model to generate a corrected token G* to complete the current round.” | 端到端 drafting/scheduling/verification 机制 | pass：重裁后 contact-sheet + 原分辨率 QA |
| DSpark / Table 1 / p.11；源页 `2481×3508`；crop `(290,330,1900,905)` | `../assets/papers/dspark/table1-main-results-caption.png` | “Main speculative decoding results. We report accepted length (τ) per decoding round (higher is better) for different target models and domains. Bold marks the best results.” | 固定 block 离线协议主结果 | pass：去除多余空白后逐图 QA |
| DSpark / Figure 2 / p.12；源页 `2481×3508`；crop `(280,350,1910,875)` | `../assets/papers/dspark/fig2-conditional-acceptance-caption.png` | “Position-wise conditional acceptance. We report the empirical conditional acceptance rate for each draft position, averaged across benchmarks within each domain using the Qwen3-4B target model. Unlike standard prefix survival, this metric isolates the baseline predictive quality at position k by removing the penalty of previous rejections. Notice that the autoregressive drafter (Eagle3) remains stable or trends upward, while the parallel drafter (DFlash) suffers suffix decay.” | suffix decay 与 sequential head 动机 | pass：2026-07-25 原分辨率 QA |
| DSpark / Figure 3 / p.13；源页 `2481×3508`；crop `(280,340,1910,945)` | `../assets/papers/dspark/fig3-drafter-depth-caption.png` | “Effect of drafter depth. With proposal length fixed, DSpark's performance improves as drafter layers are added. Notably, a shallow 2-layer DSpark outperforms a deeper 5-layer DFlash baseline, highlighting the parameter efficiency of sequential modeling.” | drafter depth 消融 | pass：caption/空白重裁后 QA |
| DSpark / Figure 4 / p.13；源页 `2481×3508`；crop `(280,1295,1910,700)` | `../assets/papers/dspark/fig4-proposal-latency-caption.png` | “Effect of proposal length and latency overhead. DSpark consistently outperforms DFlash across various block sizes (left three panels). The rightmost panel demonstrates that the sequential head introduces minimal latency overhead during serving.” | proposal length 与 latency | pass：排除上一对象 caption 后 QA |
| DSpark / Figure 5 / p.15；源页 `2481×3508`；crop `(280,340,1910,835)` | `../assets/papers/dspark/fig5-confidence-sweep-caption.png` | “Confidence threshold sweep. A threshold of 0 corresponds to standard fixed-length verification. As the threshold increases, the overall acceptance rate steadily rises because the confidence head effectively prunes tokens that would ultimately be rejected (hashed bars).” | confidence threshold diagnostic | pass：2026-07-25 原分辨率 QA |
| DSpark / Figure 6 / p.15；源页 `2481×3508`；crop `(280,1205,1910,945)` | `../assets/papers/dspark/fig6-reliability-caption.png` | “The Reliability Diagram on Alpaca Dataset. While the raw confidence estimator achieves strong discrimination, its predictions are inherently overconfident. Applying post-hoc calibration helps to align the prefix survival probabilities with empirical acceptance rates. The shaded background histogram represents the frequency distribution of sample counts across different confidence bins.” | calibration / STS 边界 | pass：caption 重裁后 QA |
| DSpark / Figure 7 / p.18；源页 `2481×3508`；crop `(280,350,1910,980)` | `../assets/papers/dspark/fig7-live-frontier-caption.png` | “Throughput vs. TPS. Aggregate output token throughput against per-request generation speed (tok/s/user) under live traffic. In our production deployment, DSpark improves the observed throughput–interactivity frontier relative to the MTP-1 baseline under the measured traffic and engine configurations.” | 线上 throughput–interactivity frontier | pass：caption/空白重裁后 QA |
| DSpark / Figure 8 / p.19；源页 `2481×3508`；crop `(280,950,1910,1350)` | `../assets/papers/dspark/fig8-load-adaptive-caption.png` | “Load-adaptive throughput and verification budgets. Top row (a, b): Aggregate output throughput across varying levels of system concurrency. Bottom row (c, d): The average target verification budget allocated per request. As concurrent load increases, the dynamic scheduler automatically restricts the per-request verification length to prevent resource contention.” | 负载自适应预算；仅支持 whole-stack 归因 | pass：排除后续正文后 QA |
| HyperDFlash | Figures 1--2 | `../assets/papers/hyperdflash/` | position acceptance、method overview | blocked: legacy PDF 页、完整 caption、bbox 与逐图 QA 待恢复 |
| JetSpec | Algorithm 1；Figures 1--4；Tables 1--12 selected | `../assets/papers/jetspec/` | tree drafting、speedup、scaling、training/tree/vLLM ablations | blocked: legacy PDF 页、完整 caption、bbox 与逐图 QA 待恢复 |
| P-EAGLE | alpha trajectory；dependency splitting；Figures 1--3 | `../assets/papers/p-eagle/` | dependency-aware drafting、architecture、mask | blocked: legacy PDF 页、完整 caption、bbox 与逐图 QA 待恢复 |
